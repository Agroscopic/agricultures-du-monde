<h2 style="text-align: center;margin-top: 5%">Atlas des Agricultures du Monde</h2>
<hr/>
<div class="col-lg-4 col-lg-offset-4">
    <div class="ibox">
        <div class="ibox-title">
            <h5>{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}</h5>
        </div>
        <div class="ibox-content">
<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset">
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-primary block full-width m-b" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
    </div>
    </div>
</div>
</form>
